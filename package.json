{
  "name": "kaliber-oauth2-server",
  "version": "1.0.0",
  "description": "## Register Application (can just be config)   - client ID (The client_id is a public identifier for apps. Even though it’s public, it’s best that it isn’t guessable by third parties)   - client secret? (The client_secret is a secret known only to the application and the authorization server. It must be sufficiently random to not be guessable)   - callback-url / redirect-url (server should never redirect to any other url)",
  "main": "index.js",
  "scripts": {
    "serve": "kaliber-serve",
    "serve:dev": "PORT=8000 CONFIG_ENV=dev kaliber-serve",
    "serve:test": "PORT=8000 CONFIG_ENV=testing kaliber-serve",
    "start": "npm-run-all --parallel watch serve:dev",
    "start:test": "CONFIG_ENV=testing npm-run-all --parallel watch:test serve:test",
    "watch": "CONFIG_ENV=dev kaliber-watch",
    "watch:test": "CONFIG_ENV=testing kaliber-watch",
    "lint": "npm-run-all --serial lint.javascript lint.styles",
    "lint.javascript": "eslint -c .eslintrc --ignore-path .gitignore './**/*.js'",
    "lint.styles": "stylelint --config .stylelintrc --ignore-path .gitignore './**/*.css'",
    "test": "CONFIG_ENV=testing jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@kaliber/build": "^0.0.114",
    "jsonwebtoken": "^8.5.1",
    "react-query": "^3.21.1"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "@types/node": "^16.7.10",
    "@types/react": "^17.0.20",
    "@types/react-dom": "^17.0.9",
    "jest": "^27.1.0",
    "node-fetch": "2.6.2",
    "typescript": "^4.4.2"
  }
}